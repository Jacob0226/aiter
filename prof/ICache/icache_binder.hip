// !!! This is a file automatically generated by hipify!!!
// icache_binder.cu

#include <pybind11/pybind11.h>
#include <hip/hip_runtime.h>
#include <ATen/hip/impl/HIPStreamMasqueradingAsCUDA.h>
#include <ATen/hip/HIPContext.h>

__global__ void flush_icache_kernel()
{
    asm __volatile__("s_icache_inv \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t"
                     "s_nop 0 \n\t" ::
                         :);
}

void flush_icache() {
    hipDeviceProp_t* prop = at::cuda::getCurrentDeviceProperties();
    int n_processor = prop->multiProcessorCount;
    dim3 grid(n_processor * 60);
    dim3 block(64);
    
    hipStream_t stream = c10::hip::getCurrentHIPStreamMasqueradingAsCUDA();
    printf("Run flush_icache(). n_processor=%d\n", n_processor);
   hipLaunchKernelGGL(( flush_icache_kernel), dim3(grid), dim3(block), 0, stream, );
    
    hipError_t err = hipGetLastError();
    if (err != hipSuccess) {
        throw std::runtime_error(
            std::string("CUDA kernel launch failed: ") + hipGetErrorString(err)
        );
    }
}


namespace py = pybind11;
PYBIND11_MODULE(icache_module, m) {
    m.doc() = "A module to flush GPU Instruction Cache."; 
    m.def("flush_icache", &flush_icache, "Flush the GPU Instruction Cache across all streaming multiprocessors.");
}